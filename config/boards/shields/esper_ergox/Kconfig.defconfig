config ZMK_BLE
	default y

config ZMK_USB
	default y

if SHIELD_ESPER_ERGOX_CENTRAL || SHIELD_ESPER_ERGOX_NORM_LEFT || SHIELD_ESPER_ERGOX_NORM_RIGHT

config ZMK_KEYBOARD_NAME
	default "Esper ErgoX"

endif

if SHIELD_ESPER_ERGOX_NORM_LEFT || SHIELD_ESPER_ERGOX_NORM_RIGHT

config ZMK_SPLIT
	default y

config ZMK_SPLIT_ROLE_CENTRAL
	default n

endif


if SHIELD_ESPER_ERGOX_CENTRAL

config ZMK_SPLIT
	default y

config ZMK_SPLIT_ROLE_CENTRAL
	default y

endif
